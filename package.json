{
  "name": "stringify-once",
  "version": "1.0.0",
  "description": "JSON.stringifyOnce method (which shows functions too and avoids circular data error)",
  "main": "dist/stringify-once.univ.js",
  "scripts": {
    "up": "npm run build && npm shrinkwrap && gitanoaddfile README.md src/stringify-once.js package.json npm-shrinkwrap.json dist/stringify-once.univ.js && gitanopush",
    "build": "./node_modules/.bin/js-export -i src/stringify-once.js -o dist/stringify-once.univ.js",
    "docs": "./node_modules/.bin/javadoc -i src/stringify-once.js -o README.md -f markdown",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "javadoc": "^1.0.3",
    "js-exporter": "^1.0.2"
  }
}
